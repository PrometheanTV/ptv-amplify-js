import API, { graphqlOperation } from "@aws-amplify/api";
import Auth from "@aws-amplify/auth";
import Amplify from "@aws-amplify/core";
import AWS from 'aws-sdk/global';

export {
    API, graphqlOperation, Auth, Amplify, AWS
};